apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-deploy

spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service-container
        image: spaul76/order-service:{{ .Values.image_tag }}
        envFrom:
        - configMapRef:
            name: order-config
        - secretRef:
            name: order-secret
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 150Mi
          limits:
            cpu: 300m
            memory: 400Mi 
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
